import 'package:flutter/material.dart';

class ImageAdminfoMovil11Widget extends StatelessWidget {
  const ImageAdminfoMovil11Widget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Image(
      image: AssetImage('assets/images/Adminfo-Movil-11.png'),
      width: 50.0,
    );
}
